import assert from 'node:assert/strict';import test from 'node:test';import { readFileSync } from 'node:fs';
test('workspace read model has pagination limits and additive indexes',()=>{const repo=readFileSync('workers/premium/repositories/d1-professional-workspace-repository.js','utf8');assert.match(repo,/clamp\(filters\.limit,25\)/);assert.match(repo,/limit\+1/);assert.match(repo,/JOIN premium_students/);const migration=readFileSync('migrations/0033_add_professional_workspace_indexes.sql','utf8');assert.match(migration,/CREATE INDEX IF NOT EXISTS idx_premium_pending_items_workspace/);assert.doesNotMatch(migration,/CREATE TABLE/);});
