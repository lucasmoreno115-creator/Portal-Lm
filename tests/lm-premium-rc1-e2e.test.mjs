import test from 'node:test'; import assert from 'node:assert/strict'; import { DatabaseSync } from 'node:sqlite';
import { presentPublicNutritionPlan } from '../workers/premium/presenters/nutrition-plan-public-presenter.js';
import { presentWorkspaceStudentSummary } from '../workers/premium/presenters/professional-workspace-student-presenter.js';
function db(){const d=new DatabaseSync(':memory:'); d.exec(`CREATE TABLE premium_students(student_id TEXT PRIMARY KEY,email TEXT,consultation_status TEXT,access_status TEXT,display_name TEXT);CREATE TABLE student_access(student_id TEXT,email TEXT,plan TEXT,plan_type TEXT);CREATE TABLE premium_anamnesis(id TEXT PRIMARY KEY,student_id TEXT,status TEXT);CREATE TABLE student_checkins(id TEXT PRIMARY KEY,student_id TEXT,week_ref TEXT,coach_status TEXT,decision_type TEXT);CREATE TABLE nutrition_plans(id TEXT PRIMARY KEY,student_id TEXT,status TEXT,is_active INTEGER,version_number INTEGER,revision INTEGER,source_feedback_id TEXT,supersedes_plan_id TEXT,private_notes TEXT,meals_json TEXT,updated_at TEXT);CREATE TABLE premium_pending_items(id TEXT PRIMARY KEY,student_id TEXT,type TEXT,status TEXT,related_entity_type TEXT,related_entity_id TEXT,resolved_at TEXT);CREATE TABLE premium_followup_entries(id TEXT PRIMARY KEY,student_id TEXT,entry_type TEXT,title TEXT,content TEXT,related_entity_type TEXT,related_entity_id TEXT);`); return d;} function all(d,q,...p){return d.prepare(q).all(...p);} function get(d,q,...p){return d.prepare(q).get(...p);} function run(d,q,...p){return d.prepare(q).run(...p);}
test('RC1 E2E integrado persiste aluno, feedback, plano, status e isola Projeto LM',()=>{const d=db(); run(d,"INSERT INTO premium_students VALUES('s1','p@x.com','NEW','ACTIVE','Premium')"); run(d,"INSERT INTO student_access VALUES('s1','p@x.com','premium','premium')"); run(d,"INSERT INTO student_access VALUES('lm1','lm@x.com','projeto_lm','projeto_lm')"); assert.equal(get(d,"SELECT consultation_status FROM premium_students WHERE student_id='s1'").consultation_status,'NEW'); run(d,"INSERT INTO premium_anamnesis VALUES('a1','s1','SUBMITTED')"); run(d,"INSERT INTO premium_pending_items VALUES('pa','s1','ANALYZE_ANAMNESIS','OPEN','premium_anamnesis','a1',NULL)"); run(d,"UPDATE premium_anamnesis SET status='ANALYZED' WHERE id='a1'"); run(d,"UPDATE premium_pending_items SET status='RESOLVED',resolved_at='2026-07-14T00:00:00Z' WHERE id='pa'"); run(d,"UPDATE premium_students SET consultation_status='ACTIVE' WHERE student_id='s1'"); run(d,"INSERT INTO student_checkins VALUES('f1','s1','2026-W28','PENDING',NULL)"); run(d,"INSERT INTO premium_pending_items VALUES('pf','s1','ANALYZE_WEEKLY_FEEDBACK','OPEN','student_checkins','f1',NULL)"); run(d,"UPDATE student_checkins SET coach_status='ANALYZED',decision_type='UPDATE_PLAN' WHERE id='f1'"); run(d,"UPDATE premium_pending_items SET status='RESOLVED',resolved_at='2026-07-14T01:00:00Z' WHERE id='pf'"); run(d,"INSERT INTO premium_followup_entries VALUES('decision:f1','s1','PROFESSIONAL_DECISION','Atualizar plano','UPDATE_PLAN','student_checkins','f1')"); run(d,"INSERT INTO premium_pending_items VALUES('pn','s1','CREATE_NUTRITION_PLAN','OPEN','student_checkins','f1',NULL)"); run(d,"INSERT INTO nutrition_plans VALUES('old','s1','PUBLISHED',1,1,1,NULL,NULL,'secret','[]','2026-07-13')"); run(d,"INSERT INTO nutrition_plans VALUES('draft','s1','DRAFT',0,2,1,'f1','old','draft secret','[]','2026-07-14')"); const cas=run(d,"UPDATE nutrition_plans SET revision=revision+1,updated_at='2026-07-14T02:00:00Z' WHERE id='draft' AND revision=1 AND status='DRAFT'"); assert.equal(cas.changes,1); const stale=run(d,"UPDATE nutrition_plans SET revision=revision+1 WHERE id='draft' AND revision=1"); assert.equal(stale.changes,0); run(d,"UPDATE nutrition_plans SET status='ARCHIVED',is_active=0 WHERE id='old'"); run(d,"UPDATE nutrition_plans SET status='PUBLISHED',is_active=1 WHERE id='draft'"); run(d,"INSERT INTO premium_followup_entries VALUES('plan-change:draft','s1','PLAN_CHANGE','Plano publicado','v2','nutrition_plans','draft')"); run(d,"UPDATE premium_pending_items SET status='RESOLVED',resolved_at='2026-07-14T03:00:00Z' WHERE id='pn'"); for(const status of ['PAUSED','ACTIVE','ENDED']){const previous=get(d,"SELECT consultation_status FROM premium_students WHERE student_id='s1'").consultation_status; run(d,"UPDATE premium_students SET consultation_status=? WHERE student_id='s1'",status); run(d,"INSERT INTO premium_followup_entries VALUES(?,?,?,?,?,?,?)",`status:${previous}:${status}`,'s1','CONSULTATION_STATUS_CHANGE','Status alterado',`${previous}->${status}`,'premium_students','s1');} assert.equal(get(d,"SELECT consultation_status FROM premium_students WHERE student_id='s1'").consultation_status,'ENDED'); assert.equal(all(d,"SELECT * FROM premium_students WHERE student_id='lm1'").length,0); assert.equal(all(d,"SELECT * FROM nutrition_plans WHERE student_id='lm1'").length,0); assert.equal(get(d,"SELECT COUNT(*) c FROM premium_followup_entries WHERE entry_type='PLAN_CHANGE'").c,1); const publicPlan=presentPublicNutritionPlan(get(d,"SELECT * FROM nutrition_plans WHERE id='draft'")); assert.equal(JSON.stringify(publicPlan).includes('draft secret'),false); const ws=presentWorkspaceStudentSummary({student_id:'s1',email:'p@x.com',consultation_status:'ENDED',access_status:'ACTIVE',open_pending_count:0}); assert.equal(ws.studentId,'s1'); assert.equal(all(d,"SELECT * FROM premium_pending_items WHERE status='OPEN'").length,0);});
