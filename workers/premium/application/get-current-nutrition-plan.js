// LM Premium 3.0 Build 5 nutrition-plan workflow use case.
export function createGetCurrentNutritionPlanUseCase({ studentRepository, nutritionPlanRepository }) { return { async execute({ student_id }) { const student = await studentRepository.findByStudentId(student_id); if (!student) return { ok:false, error:'STUDENT_NOT_FOUND' }; const current = await nutritionPlanRepository.findCurrentByStudentId(student_id); return { ok:true, data: current || null }; } }; }
