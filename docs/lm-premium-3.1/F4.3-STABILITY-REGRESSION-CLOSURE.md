# F4.3 — Stability Regression Closure

## Executive Result

**VALIDATED**

O gate consolidado `STABILITY_REGRESSION_CLOSURE` terminou com os nove checks obrigatórios em `PASSED`. Não foram encontradas regressões funcionais S0/S1 e esta entrega não altera runtime, produto, lifecycle, schema, endpoint ou migration.

## Source SHA

`c6f87f7` — F4.2 já integrada sobre F4.1 (`9bcdfb7`).

## Scope

O fechamento reutiliza as suites HTTP + `SqliteD1` existentes e cobre lifecycle, Workspace/Prontuário, anamnese, Nutrition, Progression, Weekly Feedback/revisão/entrega, desativação, reativação, identidade canônica e isolamento do Projeto LM. O comando canônico é:

```sh
npm run qa:lm:stability-closure
```

O runner falha fechado: qualquer suite não executável ou não aprovada produz somente `NOT_VALIDATED` e exit code diferente de zero.

## Required Invariants

| Check consolidado | Evidência reutilizada | Resultado |
| --- | --- | --- |
| lifecycle | `lm-premium-lifecycle-e2e-closure` | PASSED |
| workspace | `lm-premium-workspace-operational-e2e`, navegação do dashboard | PASSED |
| anamnesis | `lm-premium-anamnesis-professional-review-e2e` | PASSED |
| nutrition | `lm-premium-nutrition-student-parity-e2e` | PASSED |
| progression | `lm-premium-progression-persistence-e2e` | PASSED |
| weeklyFeedback | professional review, student delivery e UX | PASSED |
| deactivation | `lm-premium-student-deactivation-e2e` | PASSED |
| reactivation | `lm-premium-student-reactivation-e2e` | PASSED |
| projectLmIsolation | lifecycle e Workspace operational E2E | PASSED |

## Lifecycle

O cenário canônico percorreu `AWAITING_ANAMNESIS → UNDER_REVIEW → READY_TO_RELEASE → ACTIVE → ENDED → ACTIVE`, incluindo analyze, mark-ready, release, deactivate e reactivate. A mesma identidade, token, dados operacionais e histórico permanecem durante o ciclo.

## Workspace

As filas distinguem onboarding, under review, ready to release, ACTIVE e ENDED, com pending-items e Weekly Feedback associados pelo `studentId`. Os CTAs e a navegação operacional para o Prontuário permanecem cobertos pelas suites reutilizadas.

## Anamnesis

Submissão, leitura profissional, `analyzed_at`, reload, resolução do pending e retry idempotente passaram. A análise permanece separada da promoção explícita para ready-to-release.

## Nutrition

O canário comprovou draft/publicação V1, V1 corrente durante o draft V2, publicação V2 e V1 arquivada no histórico. O DTO público preservou title, goal, strategy, meals/guidance, substitutions, observations, hydration, supplements, adherence rules, versão e publicação, sem os campos privados enumerados no contrato. A suite também prova separação entre alunos.

A jornada canônica continua em `portal-premium-nutrition-plan.html`, consumindo `GET /api/portal/premium/nutrition-plan/current`; os contratos de Home/menu e das cópias deployáveis permanecem cobertos por `npm test` e pelo gate S0.7.

## Progression

O round-trip calculator → recommendation/executionQuality → POST → GET/history passou de forma lossless. Os canários de boa e má execução e a leitura compatível de `decision`/`rir` legados permanecem cobertos sem confundir RIR com qualidade de execução. A suite prova separação entre alunos.

## Weekly Feedback

O fluxo determinístico validou submit, `checkinId` canônico, exatamente um pending aberto, detalhe e decisão profissional, resposta persistida após reload, pending resolvido, retry idempotente e entrega ao aluno no mesmo ID com `{ message, respondedAt }`. A UI atual não coleta `waist`/`abdomen` e oferece retorno à navegação Premium; campos históricos continuam apenas como compatibilidade de leitura. A suite prova separação entre alunos.

Não houve novo staging FULL_RESPONSE_FLOW: F4.1 alterou exclusivamente o contrato de Progression e F4.2 o presenter/consumer de Nutrition; nenhum deles alterou o runtime de Weekly Feedback. A regressão E2E determinística passou e a evidência de staging F2.1.1 permanece aplicável.

## Deactivation / Reactivation

Em `ENDED`, o conteúdo Premium protegido retorna bloqueio, sem hard delete de identidade, planning, anamnese, nutrition, check-ins ou histórico. A reativação restaura `ACTIVE`, o acesso e os mesmos registros, inclusive o plano publicado, sem duplicação.

## Canonical Identity

Workspace, Prontuário, anamnese, nutrition, check-in, revisão, desativação e reativação preservaram o mesmo `studentId`. Os testes exercitam autenticação compartilhada e impedem que fallback por e-mail crie uma nova identidade operacional.

## Project LM Isolation

O cenário dual-product captura o perfil do Projeto LM antes das mutações Premium e compara a row completa depois de anamnese, trabalho semanal, desativação e reativação. O estado é `deepEqual`, e o acesso ao Projeto LM permanece disponível durante `ENDED` Premium.

## Regression Budgets

`workerApiBytes = 292866`, dentro do máximo imutável de `293000`. Todos os ERROR budgets passam no head final; thresholds não foram alterados.

## CI

Os equivalentes locais dos required gates são `npm test`, `npm run qa:lm:workspace-entry`, `npm run qa:lm:premium-api`, `npm run check:project-lm-runtime`, `npm run baseline:check` e a cobertura S0.7. Todos devem permanecer verdes no head hospedado da PR; nenhum check é opcional.

## Known Accepted Non-Blockers

Continuam fora deste fechamento, conforme F4.0: professional progression read loop, weekly objectives version history, MFIT read-model, library administration, Home progress widget e legacy runtime cleanup. Nenhum bug funcional atual foi identificado nesses itens. S0 open: **0**. S1 functional regression open: **0**.

## Final Verdict

## VALIDATED

`executionMode = STABILITY_REGRESSION_CLOSURE`, todos os invariants obrigatórios passaram, os budgets permanecem dentro do limite e não há S0/S1 funcional conhecido.

**F4 — STABILITY — STATUS: CLOSED.**
