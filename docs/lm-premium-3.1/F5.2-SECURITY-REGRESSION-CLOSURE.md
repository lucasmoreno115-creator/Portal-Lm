# F5.2 — Security Regression Closure

## Executive Result

**VALIDATED**

```json
{
  "executionMode": "SECURITY_REGRESSION_CLOSURE",
  "status": "VALIDATED"
}
```

F5 — SECURITY is **CLOSED** for the Portal LM V1 scope. This is a regression verdict over the finite boundaries audited in F5.0, not a claim of absolute security. The next official block is F6 — QUALITY / STUDENT UX; no F5.3 is created.

## Source SHA

Source baseline (current main after F5.1): `9422db33f90eb44b3a42c567a9ebcabed0ef2259`.

## Security Baseline

The F5.2 runner reused the deterministic F5.0 audit and the established Premium lifecycle, nutrition, feedback, anamnesis, DTO, and Project LM isolation suites. It executed each unique evidence file once and failed closed if any mandatory file failed. No production runtime, migration, schema, threshold, authentication design, or architecture was changed.

Closure output:

| Mandatory check | Result |
|---|---|
| authentication | PASSED |
| studentIsolation | PASSED |
| adminAuthorization | PASSED |
| nutritionPrivacy | PASSED |
| progressionOwnership | PASSED |
| weeklyFeedbackOwnership | PASSED |
| anamnesisAuthorization | PASSED |
| lifecycleAuthorization | PASSED |
| deactivationReactivation | PASSED |
| massAssignment | PASSED |
| storedXss | PASSED |
| sensitiveDtos | PASSED |
| projectLmIsolation | PASSED |

The focused F5.0 SQL-binding, controlled-error, and tracked-secret regression evidence also passed. F5.1 introduced frontend renderer changes only; the closure found no dynamic SQL, public detailed exception, or credential regression in that finite scope.

## F5.0 Findings

* **SEC-PREM-001** — Original severity: **S2** — Status: **FIXED**.
* **SEC-PREM-002** — Original severity: **S2** — Status: **FIXED**.

The original finding history and severity remain recorded; closure changes remediation status only.

## F5.1 Fix Verification

The benign marker `<svg data-security-test="xss">` remained verbatim through the real check-in and progression API persistence/read paths. Both legacy renderers continued to construct nodes and assign dynamic values through `textContent` or text nodes, with no dynamic `hist.innerHTML` sink.

## Authentication

Protected Premium endpoints denied missing and invalid student credentials. Valid credentials continued into the existing product and lifecycle authorization rules.

## Authorization

Critical administrative mutations remained behind the shared admin boundary. Unauthenticated and student-authenticated requests were denied, while valid professional/admin requests followed the existing contract. Client-supplied IDs, email, lifecycle, role, status, timestamps, and other internal fields did not elevate privileges or replace server-derived ownership/metadata.

## Student Isolation

The Student A × Student B harness proved that tampered student IDs and emails could neither select nor mutate B's progression data from A's authenticated context. The reused nutrition, weekly feedback, anamnesis, lifecycle, and presenter evidence preserved the same student-scoped identity boundary.

## Nutrition Privacy

Student reads returned only the authenticated student's active published plan through the public presenter. Another student's plan, the student's own draft marker, private notes, owner identifiers, publisher, source feedback, and administrative metadata remained absent. The V1 published → V2 draft → V2 published suite confirmed that drafts never became current before publication.

## Progression Ownership

Progression POST derived ownership from validated student credentials. Injected `studentId`, `student_id`, email, ID, timestamp, and decision fields neither changed ownership nor protected metadata; GET remained scoped to the authenticated student.

## Weekly Feedback

Current/history and professional review evidence preserved student ownership, safe ID handling, and canonical rendering. `professionalResponse` remained exactly the public response contract `{ message, respondedAt }`, without administrative metadata. Persisted professional copy continued to use the canonical safe text sink.

## Anamnesis

Anamnesis submission remained authenticated and self-scoped, rejected client identity fields, and obeyed onboarding lifecycle constraints. Analysis and analyzed metadata remained professional/admin-controlled; student and Project LM-only identities could not invoke the professional mutation.

## Lifecycle

Students could not choose administrative Premium lifecycle states. The real HTTP/D1 lifecycle suite reconfirmed that transitions remained server-side and professional-controlled.

## Deactivation / Reactivation

Student credentials could invoke neither command. Authorized deactivation changed ACTIVE to ENDED and immediately blocked protected Premium content; authorized reactivation restored ACTIVE while preserving canonical identity, history, timestamps, and replay behavior.

## Stored XSS Regression

**SEC-PREM-001** and **SEC-PREM-002** were not reproducible. The check-in, progression, coach reply, and canonical Weekly Feedback render checks treated persisted strings as text rather than arbitrary markup, while the API continued to preserve the original text contract.

## Sensitive DTOs

Nutrition and Weekly Feedback continued to use explicit public presenters. Progression and applicable anamnesis responses did not regress to unrestricted raw database rows. Private ownership, operational, publisher, source, and administrative fields remained outside student DTOs.

## Project LM Isolation

The dual-product lifecycle evidence snapshot Project LM state around Premium mutations and preserved the invariant equivalent to `deepEqual(before, after)`. Shared canonical identity did not merge product entitlement or lifecycle: Premium deactivate/reactivate left Project LM authorization and state unchanged.

## Stability Closure

`npm run qa:lm:stability-closure` returned `executionMode: STABILITY_REGRESSION_CLOSURE`, `status: VALIDATED`, with every F4 mandatory domain passed. F4 remains closed.

## CI

Local Node `v22.22.2` evidence on the closure head:

| Gate / command | Result |
|---|---|
| `npm run qa:lm:security-closure` | VALIDATED; 13/13 mandatory checks PASSED |
| `npm run qa:lm:security-audit` | PASS; 4/4 tests |
| `npm run qa:lm:stability-closure` | VALIDATED |
| `npm test` | PASS; 1361 passed, 0 failed |
| `npm run db:expected` | PASS; no migration/schema delta |
| `npm run baseline:check` | PASS; 10 error budgets passed, 0 errors |
| `npm run check:project-lm-runtime` | PASS |
| `npm run qa:lm:workspace-entry` | PASS; 17/17 tests |
| `npm run qa:lm:premium-api` | VALIDATED |

The corresponding required checks remain enforced on the PR head: **Agente QA LM**, **Project LM Quality Gate**, **Portal performance baseline**, and **Portal Home resource S0.7**.

## Open Security Backlog

* S0 OPEN = **0**
* S1 OPEN = **0**
* S2 REQUIRED_FOR_V1 OPEN = **0**
* SEC-PREM-001 = **FIXED**
* SEC-PREM-002 = **FIXED**

## Final Verdict

`executionMode = SECURITY_REGRESSION_CLOSURE` and `status = VALIDATED`.

Os boundaries de segurança definidos e auditados para o escopo V1 foram regressivamente validados; os dois findings confirmados da F5.0 foram corrigidos; não existem S0/S1 ou S2 REQUIRED_FOR_V1 abertos; o isolamento entre alunos e produtos permanece preservado.

**F5 — SECURITY — STATUS: CLOSED.**
