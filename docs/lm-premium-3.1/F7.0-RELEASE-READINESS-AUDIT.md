# F7.0 — Portal LM V1 Release Readiness Audit

## Target

- **Audit target:** `auditTarget = main@802c394fee1eefad93cb215574d5b3d71c81e087`.
- **Audited commit:** `802c394fee1eefad93cb215574d5b3d71c81e087` — merge commit for F6.3 / PR #465.
- **Checkout exposed by the audit environment:** local branch `work`, pointing exactly at the target commit. The checkout has no local `main`, remote, or remote-tracking refs; no result from another SHA was mixed into this audit.
- **Initial/final product tree:** clean. `npm run db:expected` changes only the generated `generatedAt` field; that audit artifact was inspected and restored before the clean test and budget runs. This report is the only intended repository change.
- **Base used:** consolidated post-F6.3 tree at the commit above.
- **Audit date/time zone:** 2026-08-15, UTC.
- **Host:** Linux; local shell environment supplied Node `v20.20.2` by default and npm `11.4.2`.
- **Validated runtime:** Node `v22.22.2` and npm `11.4.2`, matching the repository's quality/deploy workflows. The Node 20 incompatibility with experimental `node:sqlite` was reproduced and classified as an environment/runtime mismatch, not a product failure; all mandatory local suites were rerun on Node 22.
- **Install:** `npm ci` completed with zero package vulnerabilities.
- **Worker/config:** Cloudflare Worker `lm-system-api`, entrypoint `workers/api.js`, compatibility date `2024-01-01`, static directory `public`, `ASSETS` binding, D1 binding `DB`, one Saturday cron, and API/admin Worker-first routing.
- **Expected schema:** deterministic replay of the legacy bootstrap plus versioned migrations and the registered `0021` replay override.
- **Migrations present:** 41 sequential repository migrations, `0004_nutrition_plans.sql` through `0044_add_progression_execution_quality.sql`; the expected-schema manifest also contains 41 migrations and ends at `0044`.

## Executive result

`READY_FOR_FINAL_VALIDATION`

The audit found **zero RELEASE_BLOCKERS** and **zero NON_BLOCKING findings**. Repository gates, schema replay, the complete automated suite, Premium lifecycle and round trips, professional weekly response, progression, Projeto LM isolation, security closure, stability closure, Student UX closure evidence, and regression budgets passed on the repository-supported Node 22 runtime.

The browser-backed performance smoke and remote GitHub Actions status could not be independently executed/queried in this container. These are explicitly retained as environment limitations for final validation, not silently treated as executed and not classified as product findings because no reproducible defect was observed.

## Gate matrix

| Gate | Status | Evidence |
| --- | --- | --- |
| Repository/runtime | PASS | Clean `npm ci`; runtime sync passed; reference/import structural suite passed 7/7; full clean suite passed 1376/1376 on Node 22. |
| Database/migrations | PASS | `npm run db:expected` replayed all 41 migrations through `0044`; schema/database tests in the clean suite passed; expected output differed only by generation timestamp and was restored. `DATABASE_INTEGRITY = VALIDATED`. |
| Auth/lifecycle | PASS | Full suite covers NEW/AWAITING_ANAMNESIS routing, UNDER_REVIEW/READY_TO_RELEASE/ACTIVE gates, pause/deactivate/reactivate, fail-closed student/admin access, and deterministic redirects. Security and stability closure gates passed. `AUTH_LIFECYCLE = VALIDATED`. |
| Premium | PASS | Clean suite exercises Home, onboarding/anamnesis, nutrition, weekly plan/check-in, progression, library/navigation/support contracts, Workspace, Student Record, and real HTTP/D1 professional-to-student and student-to-professional round trips. |
| Professional response | PASS | Security/stability orchestration reran real HTTP/D1 weekly-feedback review, canonical identity/check-in ID, response persistence, history, retry/idempotency, student availability, and Projeto LM exclusion. |
| Progression | PASS | F6.2/F6.3 tests in the clean suite prove one POST under double activation, disabled/saving/success/error/retry save states, and delayed/empty/populated/failed history without unhandled rejection; canonical POST→DB→GET closure passed 4/4. |
| Projeto LM | PASS | Full suite plus runtime/dependency structural suite cover onboarding, journey/home, daily/weekly actions, training, nutrition, check-ins, continuity, library, persistence, navigation, frozen contracts, and synchronized deployable assets. `PROJECT_LM = VALIDATED`. |
| Isolation | PASS | Full, security, and stability suites prove Premium/Admin assets and APIs do not cross into Projeto LM/LM2/V5 state, mutations, lifecycle, or storage; shared identity retains independent product authorization/state. `CROSS_PRODUCT_ISOLATION = VALIDATED`. |
| Security | PASS | `qa:lm:security-closure`: 29/29 tests across six sub-suites; authentication, authorization, student ownership, mass assignment, sensitive DTOs, stored XSS, lifecycle, and cross-product isolation all `PASSED`. `SECURITY = VALIDATED`. |
| Stability | PASS | `qa:lm:stability-closure`: 46/46 tests across nine sub-suites; lifecycle, Workspace, anamnesis, nutrition, progression, weekly feedback, deactivation/reactivation, persistence, retries, and isolation all `PASSED`. `STABILITY = VALIDATED`. |
| Student UX | PASS | `qa:lm:student-ux-audit` exited 0 with all canonical surfaces/destinations and lifecycle, navigation, mutation guard, loading, progression error, and home-loading checks true. The frozen UX-PREM-001..004 changes are present and their F6 regression tests pass: `4/4 FIXED`. The script's historical `UX_GAPS_FOUND` mode label names the frozen audit baseline; it is not a failed check. |
| Budgets | PASS | Clean `baseline:check` exited 0: 10 pass, 0 error, 3 warning. `publicHtmlBytes` is 281,500 / 281,500 (0-byte margin). All severity-error budgets passed. |
| Performance | PASS | Official technical baseline/budget gate passed and performance contract/analysis tests passed 60/60. Real-Chrome smoke is `ENVIRONMENT_BLOCKED` because this container has no Chrome executable; no performance gate violation was observed. `PERFORMANCE = VALIDATED` for locally executable evidence, with browser measurement retained for F7.2. |
| Deploy readiness | PASS | Deploy workflow uses Node 22, clean install/runtime/full-suite gates, main-only production deployment, pinned Wrangler 4.65.0, production environment, D1/assets bindings, commit→Worker version metadata, retained artifact, and post-deploy API/static/redirect smoke. No deploy was triggered. `DEPLOY_READINESS = VALIDATED`. |

## Database and migration integrity

The expected-schema command successfully replayed the bootstrap, every migration, and the registered replay override using Node 22. The migration filenames are sequential in the repository's established range and no duplicate migration number was observed. The expected manifest and filesystem both report 41 migrations and the same terminal migration (`0044_add_progression_execution_quality.sql`). Runtime/schema compatibility is additionally exercised by the full suite's SQLite-backed repository and end-to-end tests.

No migration was created or edited. No staging or production D1 mutation was attempted.

`DATABASE_INTEGRITY = VALIDATED`

## Critical contract coverage

### Premium and lifecycle

The clean suite supplies executable coverage for protected access-state routing, initial access/anamnesis submission, professional analysis, planning readiness/release, ACTIVE portal access, pause/deactivation/reactivation, Workspace and Student Record authorization, and independent product state. Protected surfaces fail closed at the API/auth layer rather than relying on visual hiding.

### Weekly professional response

The closure suites exercise the real flow:

`student → canonical check-in → professional review/response → persistence → student current/history`

Evidence covers canonical identity and check-in ownership, pending state, all approved professional decisions, durable response, resolved derived effects, history/reload, identical retry idempotency, divergent retry conflict, safe public DTOs, and Projeto LM exclusion.

### Progression

The F6.2 and F6.3 regression tests are part of the passing clean suite. Save behavior includes duplicate activation protection, pending disabled state and `Salvando...`, visible success, controlled failure, and retry. History behavior includes delayed loading, empty and populated results, controlled GET rejection, preserved calculator usability, root/public equivalence, and no unhandled rejection. The F4.1 persistence suite independently passed its canonical POST→database→GET and compatibility cases.

### Projeto LM and isolation

Projeto LM runtime manifests/checksums, browser imports, entrypoints, internal assets, route/state contracts, lifecycle, daily/weekly persistence, training/nutrition and journey integration are covered by the clean suite and the 7-test structural check. Isolation tests explicitly reject Premium, Admin, V5, LM2, and shared-route crossings where prohibited. Dual-product lifecycle tests prove that ending/reactivating Premium does not mutate the same identity's Projeto LM profile or authorization.

## Security closure

The security closure returned `SECURITY_REGRESSION_CLOSURE / VALIDATED`. Its six sub-suites total 29 passed, zero failed, zero skipped. The executable boundary includes authentication, admin authorization, student A/B ownership, nutrition privacy, progression and weekly-feedback ownership, anamnesis/lifecycle authorization, reversible lifecycle, mass assignment, stored XSS safe sinks, sensitive DTOs, and Projeto LM isolation.

Targeted persisted-content tests retain attack-marker text as data while legacy renderers use safe DOM sinks. No exploitable stored XSS or dynamic untrusted-content `innerHTML` regression was reproduced.

`SECURITY = VALIDATED`

## Regression budgets

| Metric | Actual | Limit/baseline | Margin | Severity | Result |
| --- | ---: | ---: | ---: | --- | --- |
| `ensureSchemaRuntimeReferences` | 3 | 3 | 0 | error | PASS |
| `largestPublicAssetBytes` | 775,360 | 800,000 | 24,640 | error | PASS |
| `publicCssBytes` | 254,750 | 254,750 | 0 | error | PASS |
| `publicHtmlBytes` | 281,500 | 281,500 | 0 | error | PASS |
| `publicJavaScriptBytes` | 461,537 | 461,635 | 98 | error | PASS |
| `selectStarInWorkers` | 0 | 0 | 0 | error | PASS |
| `workerApiBytes` | 292,866 | 293,000 | 134 | error | PASS |
| `publicBytes` | 16,728,795 | 17,005,000 | 276,205 | warning | PASS |

Warnings are inventory observations, not violated budgets: Worker route inventory is 152 versus baseline 148; migrations are 41 versus baseline 37; root/public divergent comparisons are 21 versus baseline 19. They have no demonstrated functional impact and therefore are not findings. The zero-byte HTML and CSS margins and 134-byte Worker margin are release-management risks, but are still within approved error budgets and are not blockers.

## Findings

### RELEASE_BLOCKER

None. Count: **0**.

### NON_BLOCKING

None. Count: **0**.

### NOT_A_FINDING observations

- Node 20 cannot load `node:sqlite`; repository CI/deploy quality is explicitly Node 22 and the entire audit passed on Node 22. This is an audit-shell mismatch, not a supported-runtime product regression.
- The Student UX command prints its frozen F6.0 audit-mode label `UX_GAPS_FOUND`, while returning success and reporting every current closure check as true. The four frozen fixes are independently covered by passing F6.1–F6.3 tests.
- Budget margins of zero for HTML/CSS and 134 bytes for the Worker merit monitoring but do not violate any approved gate.
- Baseline inventory warnings do not demonstrate broken behavior and are not promoted into findings.

## Test inventory

All counts below are real command results for the audited SHA. A failed preliminary run is retained for transparency and was not used as the supported-runtime verdict.

| Command / suite | Tests | Pass | Fail | Skipped | Status / notes |
| --- | ---: | ---: | ---: | ---: | --- |
| `npm ci` | — | — | — | — | PASS; clean install, 0 vulnerabilities. |
| `npm run check:project-lm-runtime` | command | 1 | 0 | 0 | PASS; runtime up to date. |
| `npm run db:expected` on Node 22 | command | 1 | 0 | 0 | PASS; 41-migration replay. Generated timestamp restored. |
| `npm test` on Node 22 with clean tree | 1,376 | 1,376 | 0 | 0 | PASS. |
| `npm run qa:lm:security-closure` | 29 | 29 | 0 | 0 | PASS; six sub-suites, status `VALIDATED`. |
| `npm run qa:lm:stability-closure` | 46 | 46 | 0 | 0 | PASS; nine sub-suites, status `VALIDATED`. |
| `npm run qa:lm:student-ux-audit` | executable checks | all true | 0 | 0 | PASS (exit 0); frozen finding IDs present and closure guards true. |
| `npm run baseline:check` with clean tree | 1,376 embedded | 1,376 | 0 | 0 | PASS; budget summary 10 pass / 0 error / 3 warning. |
| `npm run performance:portal:test` | 60 | 60 | 0 | 0 | PASS. |
| `npm run performance:portal:smoke` | — | — | — | — | `ENVIRONMENT_BLOCKED`: no Chrome executable in the container. |
| `node --test tests/project-lm-runtime-sync.test.mjs tests/v4-dependency-inventory.test.mjs` | 7 | 7 | 0 | 0 | PASS. |
| Preliminary `npm run db:expected` / `npm test` on default Node 20 | 1,225 reported | 1,200 | 25 | 0 | `ENVIRONMENT_BLOCKED`, not product `FAILED`: `node:sqlite` is unavailable in Node 20. Rerun on workflow Node 22 passed. |
| Preliminary Node 22 `npm test` while generated schema timestamp was still modified | 1,376 | 1,375 | 1 | 0 | Audit setup artifact: a scope-guard test correctly detected the dirty tree. After restoring the generated timestamp, clean rerun passed 1,376/1,376. |

## Deploy and CI inspection

Static inspection validated the production workflow contract:

1. pushes to `main` start the deploy workflow;
2. quality uses Node 22, `npm ci`, Project LM runtime sync, import/dependency tests, the full suite, and a clean-tree assertion;
3. deploy depends on quality and is restricted to a `main` push and the protected `production` environment;
4. Wrangler is pinned to 4.65.0 and consumes the Cloudflare API token by secret reference without exposing a value;
5. `wrangler.toml` provides the Worker entrypoint, assets binding/directory, D1 binding and cron;
6. deployment output is converted into metadata binding the Git SHA, Worker name, and Cloudflare version ID, then retained as an artifact;
7. post-deploy smoke validates health, root and critical Premium static surfaces, cache-busted assets, canonical redirects, and API/static 404 behavior.

No production publish, remote D1 command, or destructive action was performed. The checkout exposes no Git remote and no GitHub token/status endpoint, so remote workflow results for the SHA could not be independently queried. Local execution reproduced the quality workflow's Node 22 gates; remote CI status remains an explicit F7.2 validation item.

## Residual risks

These are separated from blockers because none is a demonstrated product defect:

1. **Browser performance evidence:** this container has no Chrome executable, so the real-CDP smoke/measurement was `ENVIRONMENT_BLOCKED`. Contract tests passed 60/60 and no budget violation occurred. F7.2 should require the dedicated Chrome-equipped workflow to be green for this SHA.
2. **Remote CI evidence:** no remote/ref or credentials were supplied. F7.2 should verify GitHub Actions for `802c394fee1eefad93cb215574d5b3d71c81e087`, especially Project LM Quality, deploy quality, and the dedicated performance workflow.
3. **Live environment fidelity:** staging/production D1 audits, authenticated live E2E commands, production smoke, and deployment were intentionally not run because this phase forbids destructive/publication side effects and no environment credentials were supplied. Local real-HTTP/SQLite D1-compatible E2E evidence is strong, but F7.2 must perform the final environment validation.
4. **Narrow budgets:** HTML and CSS have no remaining byte margin and Worker API has 134 bytes. Current gates are green; subsequent changes must not alter budgets merely to pass.
5. **Expected-schema artifact timestamp:** `db:expected` rewrites `generatedAt` even when schema structure is unchanged. The generated structural content was validated and the timestamp-only diff restored so audit documentation remains the sole change.

## Final decision

`F7.0 = PASSED`

`releaseReadiness = READY_FOR_FINAL_VALIDATION`

**Is Portal LM V1 ready to proceed to final release validation?**

`YES — READY_FOR_FINAL_VALIDATION`

There are zero frozen release blockers. The next step is **F7.2 final validation**, including remote green CI, Chrome-backed performance evidence, and non-destructive staging/release-fidelity checks for the exact audited SHA.
