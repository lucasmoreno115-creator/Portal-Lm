# F7.2 — Final Release Validation

## Release target

- **Validation phase:** `FINAL VALIDATION`.
- **`main` release SHA supplied by the checkout:** `100bdbaf3aa8ebcfd47ec153a29434f88dede93f` (`docs: audit Portal LM V1 release readiness (#466)`). The checkout has only the local branch `work`; it has no local `main`, remote, or remote-tracking ref with which to independently confirm the branch tip.
- **Audited commit:** `100bdbaf3aa8ebcfd47ec153a29434f88dede93f`.
- **Deploy commit/run:** unavailable; the checkout has no Git remote and `gh auth status` reports no authenticated GitHub host.
- **Deploy workflow:** `.github/workflows/cloudflare-deploy.yml` (`Deploy Cloudflare Worker`), statically identified but not remotely verified for this SHA.
- **Worker/environment:** configured as `lm-system-api` / `production`.
- **Worker version ID:** unavailable.
- **Declared production hostname:** `portal.lucasmorenopersonal.com.br`.
- **Preview/runtime target:** not selected. Per the fidelity rule, the declared hostname was not treated as a validation target without deploy/version evidence.
- **Deploy metadata artifact:** unavailable. The workflow contract would produce `cloudflare-worker-version-<sha>/worker-deploy-metadata.json`, but no artifact or run ID is accessible here.
- **Validation started:** `2026-08-15T14:38:07Z`.
- **Machine-readable evidence:** `F7.2-final-release-metadata.json` in this directory.

## Fidelity chain

```text
100bdbaf3aa8ebcfd47ec153a29434f88dede93f
  → UNKNOWN_DEPLOY_RUN
  → UNKNOWN_WORKER_VERSION
  → portal.lucasmorenopersonal.com.br (declared, not proven)
  → UNVERIFIED_RUNTIME_TEST_TARGET
```

The required `SHA → deploy → version → target` chain is incomplete. No live/static/API/authenticated test was attributed to production because doing so would validate an environment whose correspondence to the release SHA is unproven.

## Repository gates

The shell supplies Node `v20.20.2`; repository workflows require Node 22. The available Node 22 installer could not resolve/download the runtime in this environment. Failures caused by the missing `node:sqlite` builtin are recorded rather than interpreted as product corrections or silently waived.

| Command | Result | Objective evidence |
| --- | --- | --- |
| `npm ci` | PASS | Exit 0; one package audited; zero vulnerabilities. |
| `npm test` | BLOCKED | Exit 1; 1,225 executed, 1,200 passed, 25 failed, 0 skipped. All reported failures originate from Node 20 lacking `node:sqlite`; the required Node 22 rerun is absent. |
| `npm run check:project-lm-runtime` | PASS | Exit 0; runtime is up to date. |
| `npm run db:expected` | BLOCKED | Exit 1, `ERR_UNKNOWN_BUILTIN_MODULE: node:sqlite` under Node 20. |
| `npm run qa:lm:security-closure` | BLOCKED | Exit 1 because its Node/SQLite suites cannot start; status `NOT_VALIDATED`. |
| `npm run qa:lm:stability-closure` | BLOCKED | Exit 1 because its Node/SQLite suites cannot start; status `NOT_VALIDATED`. |
| `npm run qa:lm:student-ux-audit` | PASS | Exit 0; all current checks true and UX-PREM-001..004 present in the frozen evidence. |
| `npm run baseline:check` | FAIL | Exit 1; 10 budget checks passed, 1 error, 3 warnings. The error is `requiredCommands`, inherited from the failed Node 20 test run. |
| `npm run performance:portal:test` | PASS | Exit 0; 60/60 passed. |
| `npm run performance:portal:smoke` | BLOCKED | Exit 1; `CHROME_REQUIRED`, with no Chrome/Chromium executable available. |
| `node --test tests/project-lm-runtime-sync.test.mjs tests/v4-dependency-inventory.test.mjs` | PASS | Exit 0; 7/7 passed. |

Therefore `ALL REQUIRED LOCAL GATES = GREEN` is **not established**.

## CI matrix

| Gate | Status |
| --- | --- |
| Project LM Quality | FAIL — remote status unavailable |
| QA LM | FAIL — remote status unavailable |
| Performance | FAIL — remote status unavailable and browser smoke not executed |
| Baseline | FAIL — local exit code 1 |
| Security | FAIL — closure not validated on required runtime; remote status unavailable |
| Stability | FAIL — closure not validated on required runtime; remote status unavailable |

No GitHub token, authenticated `gh` session, remote URL, Actions run data, branch-protection data, or deploy artifact is present. Consequently `ALL REQUIRED CI = SUCCESS` cannot be asserted.

## E2E matrix

| Flow | Result |
| --- | --- |
| Premium lifecycle | BLOCKED |
| Anamnese | BLOCKED |
| Nutrition | BLOCKED |
| Weekly Check-in | BLOCKED |
| Professional Response | BLOCKED |
| Progression | BLOCKED |
| Workspace | BLOCKED |
| Projeto LM | BLOCKED |
| Cross-product isolation | BLOCKED |

The secure staging URL, admin/student credentials, canonical fixture identifiers, and Cloudflare/Wrangler access required by the approved staging harness are absent. More importantly, no deploy/version fidelity was proven. The E2Es, static smoke, and `/api/health` were therefore not run against or credited to an unproven target. `unexpectedCalls` is **not measured**, rather than incorrectly reported as an empty array.

## Database fidelity

`DATABASE_FIDELITY = BLOCKED`. The configured D1 binding is `DB`, database name `lmsystemv2-db`, and the repository terminal migration is `0044_add_progression_execution_quality.sql`. However, no authenticated, read-only remote audit could prove applied migrations, terminal migration, tables/columns, or absence of drift on the release target. No database mutation was attempted.

## Security

`SECURITY = BLOCKED`. The closure command was invoked but could not execute its SQLite-backed suites on Node 20, and neither required-runtime CI evidence nor final-target ownership/authorization/XSS regression evidence is accessible. No security defect is inferred from the runtime limitation; the mandatory validation is simply absent.

## Stability

`STABILITY = BLOCKED`. The closure command was invoked but could not execute its SQLite-backed suites on Node 20. Target-backed retries, idempotency, lifecycle, persistence, reload, canonical IDs, student isolation, and product isolation remain unvalidated for this release deployment.

## Student UX regression

The local audit exited 0 with all current closure checks true and retained `UX-PREM-001`, `UX-PREM-002`, `UX-PREM-003`, and `UX-PREM-004` in its frozen evidence. This local pass does not override the absent final-target E2E evidence.

## Performance

`BROWSER_PERFORMANCE = BLOCKED`. Performance contract tests passed 60/60, but the mandatory real-browser smoke exited 1 because Chrome/Chromium is unavailable. No thresholds or budgets were changed.

The measured regression inventory is unchanged from the audited tree:

| Metric | Actual | Approved maximum/baseline | Gate result |
| --- | ---: | ---: | --- |
| `publicHtmlBytes` | 281,500 | 281,500 | PASS |
| `publicCssBytes` | 254,750 | 254,750 | PASS |
| `publicJavaScriptBytes` | 461,537 | 461,635 | PASS |
| `workerApiBytes` | 292,866 | 293,000 | PASS |
| `largestPublicAssetBytes` | 775,360 | 800,000 | PASS |
| `publicBytes` | 16,728,795 | 17,005,000 (warning) | PASS |

The overall baseline command still failed because its required-command verdict contains the Node 20 test failure. Budgets were not increased.

## Residual risks

None are classified as merely residual: every missing mandatory final-validation item below blocks the release decision.

## Final blockers

1. **`FINAL_RELEASE_BLOCKER — RELEASE_TARGET_FIDELITY`:** no GitHub deploy run, deploy SHA, Worker version ID, or metadata artifact is available, so the release target cannot be bound to `100bdbaf...`.
2. **`FINAL_RELEASE_BLOCKER — REQUIRED_CI_UNVERIFIED`:** real GitHub required-check and workflow statuses for the SHA cannot be queried.
3. **`FINAL_RELEASE_BLOCKER — REQUIRED_LOCAL_GATES_NOT_GREEN`:** the required Node 22 runtime is unavailable; `npm test`, schema replay, security/stability closure, and the aggregate baseline are not green in this environment.
4. **`FINAL_RELEASE_BLOCKER — BROWSER_PERFORMANCE_UNVALIDATED`:** the official real-browser smoke cannot run because Chrome/Chromium is unavailable, and no equivalent green workflow result is accessible.
5. **`FINAL_RELEASE_BLOCKER — TARGET_E2E_AND_DATABASE_UNVALIDATED`:** target fidelity and secure staging/Cloudflare inputs are absent, preventing static/API health, authenticated Premium/Projeto LM E2Es, cross-product call capture, and read-only D1 fidelity audit.

No product code, contract, migration, threshold, or budget was changed. No blocker was corrected in this validation PR.

## Final decision

**Is Portal LM V1 ready for release?**

`RELEASE_BLOCKED`

`F7.2 = BLOCKED`

`releaseStatus = RELEASE_BLOCKED`

`FINAL_RELEASE_BLOCKERS = 5`

`PORTAL_LM_V1 = NOT_CLOSED`

---

## F7.2-fix1 — Final Evidence Closure

### Historical context and SHA separation

The initial F7.2 execution above remains the immutable record of the first attempt. It was blocked because the shell selected Node 20, GitHub/deploy evidence and staging credentials were absent, and Chrome was unavailable. Fix1 found the already-installed supported runtime and reran the local gates; it did not obtain or invent the external evidence that remains unavailable.

- **`productReleaseSha`:** `100bdbaf3aa8ebcfd47ec153a29434f88dede93f` (`main` supplied as the product release base).
- **`evidencePrHeadSha`:** `80409fe2f09e1d7ff56871f5d6a2c9e8a1c90469` (PR #467 HEAD before this fix1 evidence update).
- **Runtime-affecting diff:** `none`. `git diff --name-status 100bdbaf...80409fe` contains only the two F7.2 documentation/evidence files; no Worker, API, HTML, CSS, JavaScript, migration, schema, budget, threshold, or test changed.
- **Fix1 validation timestamp:** `2026-08-15T16:17:55Z`.
- **Supported runtime:** Node `v22.22.2`, npm `11.4.2`.

The earlier Node `v20.20.2` attempt (1,225 executed / 1,200 passed / 25 failed) is retained above and in metadata as `INVALID_FOR_SUPPORTED_RUNTIME`; it is neither hidden nor counted as a product failure.

### Evidence obtained in fix1

| Command | Exit | Tests | Pass | Fail | Skipped | Fix1 result |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| `npm ci` | 0 | — | — | — | — | PASS; zero vulnerabilities |
| `npm test` | 0 | 1,376 | 1,376 | 0 | 0 | PASS |
| `npm run check:project-lm-runtime` | 0 | command | 1 | 0 | 0 | PASS |
| `npm run db:expected` | 0 | command | 1 | 0 | 0 | PASS; 41 migrations through `0044` |
| `npm run qa:lm:security-closure` | 0 | 29 | 29 | 0 | 0 | `VALIDATED` |
| `npm run qa:lm:stability-closure` | 0 | 46 | 46 | 0 | 0 | `VALIDATED` |
| `npm run qa:lm:student-ux-audit` | 0 | executable checks | all | 0 | 0 | PASS; frozen UX-PREM-001..004 retained |
| `npm run baseline:check` | 0 | 1,376 embedded | 1,376 | 0 | 0 | PASS; 10 pass / 0 error / 3 warning |
| `npm run performance:portal:test` | 0 | 60 | 60 | 0 | 0 | PASS |
| `node --test tests/project-lm-runtime-sync.test.mjs tests/v4-dependency-inventory.test.mjs` | 0 | 7 | 7 | 0 | 0 | PASS |

The schema generator's timestamp-only change was inspected and restored before the clean full-suite and baseline executions. `ALL_REQUIRED_LOCAL_GATES = GREEN` and `REQUIRED_LOCAL_GATES = VALIDATED` for the supported runtime.

A preliminary Node 22 full-suite run overlapped the generated schema artifact and correctly failed one scope test because the tree was dirty. It was an invalid audit setup run, not the final gate. After restoring the generated timestamp and running without overlap, the clean rerun passed 1,376/1,376. This intermediate attempt is disclosed here rather than silently omitted.

### Regression budgets on Node 22

| Metric | Actual | Approved maximum/baseline | Result |
| --- | ---: | ---: | --- |
| `publicHtmlBytes` | 281,500 | 281,500 | PASS |
| `publicCssBytes` | 254,750 | 254,750 | PASS |
| `publicJavaScriptBytes` | 461,537 | 461,635 | PASS |
| `workerApiBytes` | 292,866 | 293,000 | PASS |
| `largestPublicAssetBytes` | 775,360 | 800,000 | PASS |
| `publicBytes` | 16,728,795 | 17,005,000 (warning) | PASS |

No limit was changed. The aggregate baseline exited 0 with zero error-budget violations.

### External evidence re-check

The checkout still has no configured Git remote or remote-tracking refs. `.git/FETCH_HEAD` identifies the repository URL, but `gh auth status` reports no authenticated GitHub host, and the GitHub API request for the private repository is unauthorized. No GitHub token, Cloudflare credential, staging URL, admin/student credential, canonical staging fixture, deploy artifact, or Chrome/Chromium executable is exposed.

Accordingly:

- no real Actions run IDs/conclusions can be recorded in `ciRuns`;
- no `worker-deploy-metadata.json`, deploy run ID, or Worker version ID can be read;
- the declared production hostname is still not accepted as proof of a version-bound target;
- the official Chrome-backed workflow result cannot be verified and local browser smoke cannot run;
- no static/API/authenticated E2E or read-only remote D1 audit is executed against an unproven target;
- `unexpectedCalls` remains `null` (not observed), never an inferred `[]`.

The current fidelity chain therefore remains:

```text
100bdbaf3aa8ebcfd47ec153a29434f88dede93f
  → UNKNOWN_DEPLOY_RUN
  → UNKNOWN_WORKER_VERSION
  → UNVERIFIED_RUNTIME_TEST_TARGET
```

### Fix1 E2E and final-target matrix

| Flow | Fix1 result |
| --- | --- |
| Static/API smoke | BLOCKED — target fidelity not proven |
| Premium lifecycle | BLOCKED — target fidelity/credentials unavailable |
| Anamnese | BLOCKED — target fidelity/credentials unavailable |
| Nutrition | BLOCKED — target fidelity/credentials unavailable |
| Weekly Check-in / Professional Response | BLOCKED — approved staging harness inputs unavailable |
| Progression | BLOCKED — target fidelity/credentials unavailable |
| Workspace | BLOCKED — target fidelity/credentials unavailable |
| Projeto LM | BLOCKED — target fidelity/credentials unavailable |
| Cross-product isolation | BLOCKED — no target call capture; `unexpectedCalls = null` |
| Database fidelity | BLOCKED — no authenticated read-only target audit |

The local Node 22 security and stability closure suites are `VALIDATED`, but the final `SECURITY` and `STABILITY` verdicts remain blocked because required CI and final-target E2E evidence are mandatory inputs.

### Blocker closure matrix

| Blocker original | Evidence | Final status |
| --- | --- | --- |
| `RELEASE_TARGET_FIDELITY` | SHA → deploy → version → target remains incomplete | **OPEN** |
| `REQUIRED_CI_UNVERIFIED` | GitHub Actions/API remains unauthenticated; no real runs available | **OPEN** |
| `REQUIRED_LOCAL_GATES_NOT_GREEN` | Node 22.22.2 gates green; full suite 1,376/1,376; baseline exit 0 | **CLOSED** |
| `BROWSER_PERFORMANCE_UNVALIDATED` | No accessible Chrome-backed workflow result or local browser | **OPEN** |
| `TARGET_E2E_AND_DATABASE_UNVALIDATED` | Target/credentials/deploy artifact unavailable; no E2E or D1 audit credited | **OPEN** |

Fix1 closes **one** original blocker using real evidence. Four remain open. Absence of external evidence has not been converted into success.

### Fix1 final decision

`F7.2-fix1 = BLOCKED`

`releaseStatus = RELEASE_BLOCKED`

`FINAL_RELEASE_BLOCKERS = 4`

`PORTAL_LM_V1 = NOT_CLOSED`

PR #467 must not be merged or recommended for merge while these four blockers remain open.
