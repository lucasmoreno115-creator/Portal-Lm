# F2.2.4 — Legacy mutation consolidation

## Caller inventory

| Caller | Classification | Resolution |
| --- | --- | --- |
| `admin-checkins.html` and deployed `public/admin-checkins.html` | `ACTIVE_PRODUCTION` | Migrated to the canonical decision command with explicit `decision_type`. |
| `admin-student.html` and deployed `public/admin-student.html` | `ACTIVE_PRODUCTION` | Migrated to the canonical decision command with explicit `decision_type`. |
| Premium weekly-feedback and Student Record runtimes | `ACTIVE_PRODUCTION` | Already used the canonical decision command; unchanged. |
| Premium endpoint, staging QA, and contract tests | `ACTIVE_TEST` | Canonical coverage retained; removal coverage added. |
| Route inventories and architecture audits under `docs/` | `LEGACY_UNUSED` | Historical documentation only; not executable callers. |
| `createAnalyzeWeeklyFeedbackUseCase`, `saveProfessionalDecision`, and `FEEDBACK_ANALYZED` production references | `LEGACY_UNUSED` | Exclusive to the removed mutation and deleted after repository-wide search found no consumer. |

No `UNKNOWN` callers were found in HTML, JavaScript runtimes, Workers, tests, QA/staging scripts, documentation, or fixtures.

## Mutative route contract

The supported production routes are `POST /api/admin/premium/weekly-feedbacks/:id/decision` and its existing alias `POST /api/admin/premium/feedbacks/:id/decision`. Both call `recordProfessionalDecision`.

`PATCH /api/admin/checkins/:id/reply` is removed. It has no adapter or independent persistence path. Historical `FEEDBACK_ANALYZED` rows are retained but no longer produced. Read compatibility for historical reviewed/replied/analyzed statuses remains unchanged.
